language: ruby
rvm:
- 2.1

script: bundle exec jekyll build
install: 
  - ls
  - cd plugins/
  - bundle install
after_success: bundle exec frontend push --headless

# branch whitelist
branches:
  only:
    - setup

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of