language: ruby
rvm:
- 2.1
script: bundle exec jekyll build && htmlproof ./frontend

# branch whitelist
branches:
  only:
  - setup

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of